# Pin the toolchain so every build — local, CI, and a third party verifying a release —
# uses the SAME rustc. A floating `stable` defeats reproducibility: a build months apart
# would use a different compiler. See docs/design/reproducible-builds.md.
[toolchain]
channel = "1.97.0"
components = ["clippy"]
